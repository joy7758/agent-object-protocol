# v0.5 combo fixtures (draft)

These fixtures are reserved for v0.5 rule-combination semantics and
end-to-end flow examples:

`resolve -> fetch manifest -> evaluate policy -> emit decision`

Files under this directory are not part of CI validation until JSON
fixtures are added.
