# v0.5 combo fixtures (draft)

These fixtures capture v0.5 rule-combination semantics and end-to-end
flow examples:

`resolve -> fetch manifest -> evaluate policy -> emit decision`

JSON fixtures under this directory are validated in CI against
`aop-object`, `aop-policy`, and `aop-policy-decision` schemas.
