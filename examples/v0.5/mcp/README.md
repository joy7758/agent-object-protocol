# v0.5 MCP interop fixtures (draft)

Reserved fixtures for documenting AOP tool schema to MCP tool mappings
and structured-result validation shapes.

Files under this directory are not part of CI validation until JSON
fixtures are added.
